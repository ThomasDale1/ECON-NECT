[project]
name = "econ-nect-solver"
version = "0.1.0"
description = "ECON NECT — microservicio CP-SAT del optimizador de planeación (S-A7)."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "ortools>=9.15",
    "pydantic>=2.9",
]

[tool.uv]
package = false

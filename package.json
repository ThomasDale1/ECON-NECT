{
  "name": "econ-nect-repo",
  "version": "0.0.0",
  "private": true,
  "description": "Raíz del repositorio. La aplicación vive en apps/web; esto solo reenvía los comandos para que un clon nuevo arranque con npm install && npm run dev.",
  "scripts": {
    "postinstall": "npm --prefix apps/web install",
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "lint": "npm --prefix apps/web run lint",
    "typecheck": "npm --prefix apps/web run typecheck",
    "test": "npm --prefix apps/web run test",
    "leer": "npm --prefix apps/web run leer"
  }
}
